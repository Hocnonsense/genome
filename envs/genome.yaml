name: genome
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # coding
  - mypy
  - black

  # interface
  - click

  # data analysis
  - pandas

  # bioinformation
  - biopython
  - bcbio-gff

  # pipeline
  - snakemake

  # pipe
  - pip
  - pip:
      - checkm-genome
