name: genome
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # coding
  - mypy
  - black

  # interface
  - click

  # data analysis
  - pandas

  # bioinformation
  - biopython
  - gffutils

  # pipeline
  - python=3.11 # fix snakemake f-string bug
  - snakemake

  # for checkm
  - hmmer
  - prodigal
  - pplacer

  # pipe
  - pip
  - pip:
      - checkm-genome
